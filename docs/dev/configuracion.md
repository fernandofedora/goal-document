# Configuración

## Variables de entorno (API)
- `DATABASE_URL` o parámetros de conexión.
- `JWT_SECRET`
- `PORT` (por defecto `4000`).

## Cliente
- Config de `api` base URL.
- Opciones de tema y estilos.

## Seeds opcionales
- Categorías y tarjetas iniciales para desarrollo.
